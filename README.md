# google-ext

extension to block conten on youtube.
after you have some words in body or href from `arr` like `"overwatch"` or `"dafran"` you will be take to google.com

```
...work in progress, still have to add local storage to add and remove custom words to check.
```
